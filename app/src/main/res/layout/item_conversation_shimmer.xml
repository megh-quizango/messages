<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Avatar placeholder -->
    <View
        android:id="@+id/shimmerAvatar"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/shimmer_circle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Name placeholder -->
    <View
        android:id="@+id/shimmerName"
        android:layout_width="140dp"
        android:layout_height="16dp"
        android:layout_marginStart="16dp"
        android:background="@drawable/shimmer_rounded"
        app:layout_constraintStart_toEndOf="@id/shimmerAvatar"
        app:layout_constraintTop_toTopOf="@id/shimmerAvatar"
        app:layout_constraintBottom_toTopOf="@id/shimmerSnippet" />

    <!-- Time placeholder -->
    <View
        android:id="@+id/shimmerTime"
        android:layout_width="48dp"
        android:layout_height="12dp"
        android:background="@drawable/shimmer_rounded"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/shimmerName" />

    <!-- Snippet placeholder -->
    <View
        android:id="@+id/shimmerSnippet"
        android:layout_width="0dp"
        android:layout_height="14dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="60dp"
        android:background="@drawable/shimmer_rounded"
        app:layout_constraintStart_toStartOf="@id/shimmerName"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/shimmerAvatar" />

</androidx.constraintlayout.widget.ConstraintLayout>
